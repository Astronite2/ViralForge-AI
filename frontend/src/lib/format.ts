export const formatDate=(value?:string)=>value?new Intl.DateTimeFormat("en",{dateStyle:"medium",timeStyle:"short"}).format(new Date(value)):"—";export const formatShortDate=(value?:string)=>value?new Intl.DateTimeFormat("en",{month:"short",day:"numeric"}).format(new Date(value)):"—";export const formatName=(value:string)=>value.replaceAll("_"," ").replace(/\b\w/g,letter=>letter.toUpperCase());export const pct=(value:number)=>`${(value<=1?value*100:value).toFixed(0)}%`;export const score=(value?:number)=>value==null?"—":value.toFixed(1);export const latestBy=<T>(items:T[],date:(item:T)=>string)=>[...items].sort((a,b)=>Date.parse(date(b))-Date.parse(date(a)))[0];
