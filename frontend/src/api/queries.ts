import{useQueries,useQuery}from"@tanstack/react-query";import{api}from"./client";
export const keys={topics:["topics"]as const,signals:["signals"]as const,decisions:["decisions"]as const,opportunities:["opportunities"]as const,connectors:["connectors"]as const,connectorStatuses:["connector-statuses"]as const,health:["health"]as const,ready:["ready"]as const};
export const useTopics=()=>useQuery({queryKey:keys.topics,queryFn:({signal})=>api.topics(signal)});export const useSignals=()=>useQuery({queryKey:keys.signals,queryFn:({signal})=>api.signals(signal)});export const useDecisions=()=>useQuery({queryKey:keys.decisions,queryFn:({signal})=>api.decisions(signal)});export const useDecision=(id?:string)=>useQuery({queryKey:["decision",id],queryFn:({signal})=>api.decision(id!,signal),enabled:Boolean(id)});export const useTopic=(id?:string)=>useQuery({queryKey:["topic",id],queryFn:({signal})=>api.topic(id!,signal),enabled:Boolean(id)});export const useTopicDecisions=(id?:string)=>useQuery({queryKey:["topic-decisions",id],queryFn:({signal})=>api.topicDecisions(id!,signal),enabled:Boolean(id)});export const useOpportunities=()=>useQuery({queryKey:keys.opportunities,queryFn:({signal})=>api.opportunities(signal)});export const useHistory=(id?:string)=>useQuery({queryKey:["history",id],queryFn:({signal})=>api.history(id!,signal),enabled:Boolean(id)});export const useGraph=(id?:string)=>useQuery({queryKey:["graph",id],queryFn:({signal})=>api.graph(id!,signal),enabled:Boolean(id)});export const useReasoningByDecision=(id?:string)=>useQuery({queryKey:["reasoning-decision",id],queryFn:({signal})=>api.reasoningByDecision(id!,signal),enabled:Boolean(id)});
export const useSystemHealth=()=>({health:useQuery({queryKey:keys.health,queryFn:({signal})=>api.health(signal),refetchInterval:30_000}),ready:useQuery({queryKey:keys.ready,queryFn:({signal})=>api.ready(signal),refetchInterval:30_000})});
export const useConnectors=()=>useQuery({queryKey:keys.connectors,queryFn:({signal})=>api.connectors(signal),refetchInterval:30_000});
export const useConnectorStatuses=()=>useQuery({queryKey:keys.connectorStatuses,queryFn:({signal})=>api.connectorStatuses(signal),refetchInterval:30_000});
export function useReasoningFeed(topicIds:string[]){const results=useQueries({queries:topicIds.slice(0,24).map(id=>({queryKey:["reasoning-topic",id],queryFn:({signal}:{signal:AbortSignal})=>api.reasoningByTopic(id,signal),staleTime:60_000}))});const data=results.flatMap(result=>result.data??[]).filter((item,index,all)=>all.findIndex(other=>other.id===item.id)===index).sort((a,b)=>Date.parse(b.created_at)-Date.parse(a.created_at));return{data,isLoading:results.some(result=>result.isLoading),isError:results.length>0&&results.every(result=>result.isError)}}
